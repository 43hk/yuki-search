# Yuki Search Edge Plugin

这是 Yuki Search 的 Edge 新标签页插件版本。

## 本地加载

1. 打开 Edge。
2. 进入 `edge://extensions/`。
3. 打开「开发人员模式」。
4. 点击「加载解压缩的扩展」。
5. 选择本目录：`plugin`。

加载后，新建标签页会打开插件内置的 `src/index.html`。

## 注意

- 插件版搜索候选直连 Google/Bing 候选接口，不访问自有服务器。
- 壁纸仍然从 `https://uapis.cn` 获取，手机端自动使用 `type=mb`。
